export function Footer() {
  return (
    <footer className="footer">
      <span>Secure PII Vault UI</span>
      <span>Credentials stay in memory only. No browser persistence is used.</span>
    </footer>
  );
}
